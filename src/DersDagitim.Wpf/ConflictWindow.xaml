<Window x:Class="DersDagitim.Wpf.ConflictWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Çakışma Kontrolü" Width="1050" Height="640" WindowState="Maximized" Background="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="SummaryText" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBlock x:Name="ExplanationText" Grid.Row="1" Foreground="#475569" Margin="0,0,0,18" TextWrapping="Wrap"/>
        <DataGrid x:Name="ConflictsGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tür" Binding="{Binding Type}" Width="150"/>
                <DataGridTextColumn Header="Gün" Binding="{Binding Day}" Width="110"/>
                <DataGridTextColumn Header="Ders saati" Binding="{Binding Slot}" Width="90"/>
                <DataGridTextColumn Header="Sınıf / Öğretmen / Kaynak" Binding="{Binding Entity}" Width="220"/>
                <DataGridTextColumn Header="Açıklama" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
