<Window x:Class="DersDagitim.Wpf.TeacherAvailabilityWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Öğretmen Uygunluk Kilitleri" Width="1120" Height="740" Background="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Haftalık uygunluk ızgarası" FontSize="26" FontWeight="SemiBold"/>
        <TextBlock x:Name="SummaryText" Grid.Row="1" Text="Öğretmen uygunluk/kilit kayıtları yükleniyor." Foreground="#475569" Margin="0,6,0,18" TextWrapping="Wrap"/>

        <Border Grid.Row="2" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="SQLite'a bağlı haftalık uygunluk kaydı" FontWeight="SemiBold" Foreground="#1E3A8A" Margin="0,0,0,8"/>
                <WrapPanel>
                    <ComboBox x:Name="TeacherCombo" Width="240" Height="30" Margin="0,0,8,8"/>
                    <ComboBox x:Name="DayCombo" Width="130" Height="30" Margin="0,0,8,8"/>
                    <ComboBox x:Name="LessonCombo" Width="90" Height="30" Margin="0,0,8,8"/>
                    <ComboBox x:Name="StatusCombo" Width="175" Height="30" Margin="0,0,8,8"/>
                    <TextBox x:Name="ReasonBox" Width="280" Height="30" Margin="0,0,8,8" ToolTip="Etiket veya gerekçe"/>
                    <Button Content="Kaydet" Click="Save_Click" Padding="14,6" Margin="0,0,8,8"/>
                    <Button Content="Seçili kaydı sil" Click="Delete_Click" Padding="14,6" Margin="0,0,8,8"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <DataGrid x:Name="AvailabilityGrid" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Öğretmen" Binding="{Binding TeacherName}" Width="220"/>
                <DataGridTextColumn Header="Gün" Binding="{Binding Day}" Width="120"/>
                <DataGridTextColumn Header="Ders saati" Binding="{Binding LessonNumber}" Width="90"/>
                <DataGridTextColumn Header="Durum" Binding="{Binding Status}" Width="180"/>
                <DataGridTextColumn Header="Etiket / gerekçe" Binding="{Binding Reason}" Width="*"/>
                <DataGridTextColumn Header="Tekrar" Binding="{Binding Recurrence}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
