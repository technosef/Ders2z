<Window x:Class="DersDagitim.Wpf.TeacherReportWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Öğretmen Programı ve Ders Yükü" Width="1200" Height="800" Background="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel>
            <StackPanel>
                <TextBlock x:Name="SchoolText" Text="Özel Tokat Dinamik Mesleki ve Teknik Anadolu Lisesi" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock x:Name="TeacherText" Text="Öğretmen seçiliyor..." FontSize="28" FontWeight="Bold"/>
                <TextBlock x:Name="YearText" Text="2025/2026" Foreground="#64748B"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Width="320">
                <TextBlock Text="Öğretmen" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <ComboBox x:Name="TeacherPicker" SelectionChanged="TeacherPicker_SelectionChanged"/>
            </StackPanel>
        </DockPanel>
        <TextBlock x:Name="SummaryText" Grid.Row="1" Text="Gerçek ASC atamalarından kişisel haftalık ders yükü hesaplanır." Foreground="#475569" Margin="0,18,0,12"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DataGrid x:Name="AssignmentsGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,0,16,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Dersin Adı" Binding="{Binding CourseName}" Width="220"/>
                    <DataGridTextColumn Header="HDS" Binding="{Binding WeeklyHours}" Width="70"/>
                    <DataGridTextColumn Header="Sınıf Adı" Binding="{Binding ClassNamesText}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <DataGrid x:Name="DutiesGrid" Grid.Column="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Bilgi alanı" Binding="{Binding Name}" Width="150"/>
                    <DataGridTextColumn Header="Bilgi" Binding="{Binding Details}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <DockPanel Grid.Row="3" Margin="0,16,0,0">
            <TextBlock Text="Toplam ders yükü:" FontWeight="SemiBold"/>
            <TextBlock x:Name="TotalText" Margin="8,0,0,0"/>
            <Button DockPanel.Dock="Right" Content="PDF / Excel dışa aktarım şablonu" Click="Export_Click" Padding="12,7"/>
        </DockPanel>
    </Grid>
</Window>
